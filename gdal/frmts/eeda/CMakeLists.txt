ADD_GDAL_DRIVER(TARGET gdal_EEDA SOURCES eeda.h eedaidataset.cpp eedadataset.cpp eedacommon.cpp)
GDAL_DRIVER_STANDARD_INCLUDES(gdal_EEDA)
TARGET_INCLUDE_DIRECTORIES(gdal_EEDA PRIVATE $<BUILD_INTERFACE:${GDAL_ROOT_SOURCE_DIR}/ogr/ogrsf_frmts/geojson>)
if(GDAL_USE_LIBJSONC_INTERNAL)
    TARGET_INCLUDE_DIRECTORIES(gdal_EEDA PRIVATE $<BUILD_INTERFACE:${GDAL_ROOT_SOURCE_DIR}/ogr/ogrsf_frmts/geojson/libjson>)
else()
    TARGET_INCLUDE_DIRECTORIES(gdal_EEDA PRIVATE ${JSONC_INCLUDE_DIRS})
    GDAL_TARGET_LINK_LIBRARIES(TARGET gdal_EEDA  LIBRARIES ${JSONC_LIBRARIES})
endif()
