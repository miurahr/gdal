ADD_GDAL_DRIVER(TARGET gdal_ECW
    SOURCES
        ecwsdk_headers.h gdal_ecw.h
        ecwdataset.cpp ecwcreatecopy.cpp ecwasyncreader.cpp jp2userbox.cpp)
GDAL_DRIVER_STANDARD_INCLUDES(gdal_ECW)
TARGET_INCLUDE_DIRECTORIES(gdal_ECW PRIVATE ${ECW_INCLUDE_DIR})
GDAL_TARGET_COMPILE_DEFINITIONS(TARGET gdal_ECW
    DEFINITIONS -DDO_NOT_USE_DEBUG_BOOL -DHAVE_ECW_BUILDNUMBER_H
                -DECWSDK_VERSION=${MAJOR_VERSION}${MINOR_VERSION}
)
GDAL_TARGET_LINK_LIBRARIES(TARGET gdal_ECW LIBRARIES ${ECW_LIBRARY})